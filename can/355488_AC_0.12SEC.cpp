#include <iostream> 
using namespace std; 
int main () { 
    int a,b; 
    cin>>a>>b; 
    if(b % 2 == 0) { 
        cout<<"black"; 
    } else if(b % 2 != 0) { 
        cout<<"white"; 
    } 
    return 0; 
} 
 
